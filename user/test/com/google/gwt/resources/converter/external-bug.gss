@media (max-width:767px) {
  .appFooter-appPanel {
    text-align: center;
  }

  .appFooter-linksPanelWithApps div {
    text-align: center !important;
  }

  .bankAccountCard {
    margin-left: 5%;
    margin-right: 5%;
    max-width: 800px;
    width: 90%;
  }

  .footer {
    position: relative;
  }

  .panel {
    padding-bottom: 0;
  }
}

@external appFooter-appPanel;
@external appFooter-linksPanelWithApps;
@external panel;

@if (is("user.agent", "safari")) {
  @external foo;
  .foo {
  }

  @if (is("locale", "en")) {
    @external bar;
    .bar {
    }
  }
  @elseif (eval('com.bar.foo()')) {
    @external foo2;
    .foo2 {
    }
  }
  @else  {
    @external baz;
    .baz {
    }
  }
}

@external foo;
@external dragdrop-proxy;
@external dragdrop-selected dragdrop-movable-panel;
