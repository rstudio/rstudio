<ui:UiBinder xmlns:ui='urn:ui:com.google.gwt.uibinder'
             xmlns:g='urn:import:com.google.gwt.user.client.ui'
             xmlns:rsw='urn:import:org.rstudio.core.client.widget'>

   <ui:with field="res"
            type="org.rstudio.studio.client.workbench.views.vcs.dialog.DiffFrame.Resources"/>

   <ui:style>
      @external editor_dark;

      .container {
         margin-top: 8px;
         border: 1px solid #c1c3c5;
      }

      /* a translucent grey so the border tracks the editor theme; scoped to
         dark themes because over white it composites to a fainter grey than
         the #c1c3c5 above (#9026) */
      .editor_dark .container {
         border-color: rgba(128, 128, 128, 0.4);
      }

      /* the file name and the "view file" link live in separate layers of the
         header row, so tint the row itself rather than either layer (#9026) */
      .headerRow {
      }

      .editor_dark .headerRow {
         background-color: rgba(255, 255, 255, 0.10);
      }
   </ui:style>

   <g:FlowPanel ui:field="container_" styleName="{style.container}">
      <g:LayoutPanel width='100%' height='27px' styleName="{style.headerRow}">
         <g:layer left='0px' right='175px'>
            <g:FlowPanel styleName="{res.styles.header}">
               <g:Image styleName="{res.styles.fileIcon}" ui:field="fileIcon_"/>
               <g:Label styleName="{res.styles.headerLabel}" ui:field="headerLabel_"/>
            </g:FlowPanel>
         </g:layer>
         <g:layer right='0px' width='175px'>
            <g:SimplePanel>
               <g:HorizontalPanel styleName="{res.styles.viewFilePanel}">
                  <rsw:DecorativeImage ui:field="separatorImage_"/>
                  <rsw:HyperlinkLabel ui:field="viewFileHyperlink_"/>
               </g:HorizontalPanel>
            </g:SimplePanel>
         </g:layer>
      </g:LayoutPanel>
   </g:FlowPanel>

</ui:UiBinder>
