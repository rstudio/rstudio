doPlot <- function()
  |
=====
doPlot <- function() {
  |
=====
  doPlot <- function() {
    |
=====
doPlot <- function()
{
  |
=====
plot(
  |
=====
plot(x,
     |
=====
plot(x,
     y,
     |
=====
plot(x,
     c(foo,
       |
=====
plot(x,
     c(foo,
       bar),
     |
=====
plot(
  x,
  c(foo,
    bar),
  |
=====
plot(x, c(foo, bar))
|
=====
plot(
  x, c(foo, bar))
|
=====
plot(x,
     c(foo, bar))
|
=====
  plot(x,
       c(foo, bar))
  |
=====
  if (foo())
    |
=====
  if (x) {
    |
=====
  if (foo(
    |
=====
  if (foo(
        bar),
      |
=====
  while (foo())
    NULL
  |
=====
  while (foo())
  {
    |
=====
  # This is kind of broken
  while (foo())
  {
    foo (
  }
  |
=====
  repeat
    foo()
  |
