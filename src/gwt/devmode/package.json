{
  "scripts": {
    "daemon-start":        "deemon --detach ts-node codeserver-daemon.ts",
    "daemon-wait":         "ts-node codeserver-wait.ts",
    "daemon-stop":         "deemon --kill ts-node codeserver-daemon.ts",
    "daemon-stream-start": "deemon ts-node codeserver-stream.ts",
    "daemon-stream-stop":  "deemon --kill ts-node codeserver-stream.ts"
  },
  "devDependencies": {
    "deemon": "^1.13.5"
  },
  "dependencies": {
    "@types/node": "^24.2.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  }
}
