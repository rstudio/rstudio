---
output: html_notebook
---

## R Notebook

This is an R Markdown Notebook.

When you execute R code inside the notebook, the results appear beneath the code. Try executing this chunk by placing your cursor inside it and pressing **Ctrl+Shift+Enter** (Cmd+Shift+Enter on OS X).

```{r}
summary(cars)
```

Add a new chunk here with **Ctrl+Alt+I** (Cmd+Option+I on OS X).

For more details on using R Markdown see <http://rmarkdown.rstudio.com>.
