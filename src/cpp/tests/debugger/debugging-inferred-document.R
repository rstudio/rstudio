
#
# Execute the function definition below with Cmd + Enter.
# Then, run foo() to run the code.
# The debugger should open into a view of the document.
#

foo <- function() {
   1 + 1
   browser()
   2 + 2
   6 + 6
   10000
   10000
   7 + 7
   8 + 8
   80000
}
