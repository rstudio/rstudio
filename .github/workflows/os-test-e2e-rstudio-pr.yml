name: "Test: E2E Playwright RStudio (PR-triggered)"

on:
  pull_request:
    paths:
      - "CMakeLists.txt"
      - "src/**"
      - "dependencies/**"
      - "cmake/**"
      - "package/**"
      - "e2e/rstudio/**"
      - ".github/actions/e2e-deps/**"
      - ".github/workflows/os-test-e2e-rstudio-pr.yml"
      - ".github/workflows/os-test-e2e-rstudio-desktop-os-windows-server-2025.yml"
      - ".github/workflows/os-test-e2e-rstudio-desktop-os-macos-26-arm64.yml"
      - ".github/workflows/os-test-e2e-rstudio-server-os-ubuntu-24.yml"

concurrency:
  group: pw-pr-${{ github.event.pull_request.number }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: write

jobs:
  # Single change-detection job shared by all e2e platforms (Windows Desktop,
  # macOS Desktop, Linux Server). Checks if C++, CMake, build scripts, or
  # dependencies changed. When only e2e tests or CI workflows changed, every
  # platform skips its source build and uses the latest daily installer
  # instead -- one shared check rather than three duplicate ones.
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      build_from_source: ${{ steps.check.outputs.build_from_source }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check for source changes requiring a build
        id: check
        run: |
          BASE=$(git merge-base HEAD origin/${{ github.base_ref }})
          CHANGED=$(git diff --name-only "$BASE" HEAD)
          echo "Changed files vs merge base with ${{ github.base_ref }}:"
          echo "$CHANGED"
          # Build from source when C++, GWT, CMake, build scripts, or
          # dependencies changed — anything that ends up in the compiled binary.
          # Matches all of package/ (CMakeLists.txt at the root, and every
          # per-platform subdir) plus the repo-root CMakeLists.txt so a
          # packaging or top-level cmake change can't silently fall through
          # to running tests against a daily.
          if echo "$CHANGED" | grep -qE '^(CMakeLists\.txt$|src/|package/|dependencies/|cmake/)'; then
            echo "Source files changed — building from source"
            echo "build_from_source=true" >> "$GITHUB_OUTPUT"
          else
            echo "No source files changed — using daily installer"
            echo "build_from_source=false" >> "$GITHUB_OUTPUT"
          fi

  windows:
    needs: detect-changes
    uses: ./.github/workflows/os-test-e2e-rstudio-desktop-os-windows-server-2025.yml
    secrets:
      CONNECT_API_KEY: ${{ secrets.CONNECT_API_KEY }}
    with:
      build_from_source: ${{ fromJSON(needs.detect-changes.outputs.build_from_source) }}
      project: desktop
      r_version: "4.5.3"
      grep_invert: "@ai"
      post_pr_comment: true

  macos:
    needs: detect-changes
    uses: ./.github/workflows/os-test-e2e-rstudio-desktop-os-macos-26-arm64.yml
    secrets:
      CONNECT_API_KEY: ${{ secrets.CONNECT_API_KEY }}
    with:
      build_from_source: ${{ fromJSON(needs.detect-changes.outputs.build_from_source) }}
      project: desktop
      r_version: "4.5.3"
      grep_invert: "@ai"
      post_pr_comment: true

  linux-server:
    needs: detect-changes
    uses: ./.github/workflows/os-test-e2e-rstudio-server-os-ubuntu-24.yml
    secrets:
      CONNECT_API_KEY: ${{ secrets.CONNECT_API_KEY }}
    with:
      build_from_source: ${{ fromJSON(needs.detect-changes.outputs.build_from_source) }}
      project: server
      r_version: "4.5.3"
      grep_invert: "@ai"
      post_pr_comment: true
