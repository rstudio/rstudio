{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build RStudio",
            "type": "shell",
            "command": "cmake --build . --target all",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "options": {
                "cwd": "build",
            },
            "problemMatcher": "$gcc",
        }
    ]
}
