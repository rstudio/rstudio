node_modules/
dist/
test-results/
# Page dumps written by the prototype flows (screenshots/HTML of the live
# GitHub pages); regenerated on every failed run.
prototypes/last-*
# Per-run HTML report dirs (playwright-report-<timestamp>) and the
# playwright-report symlink that points at the most recent one.
playwright-report
playwright-report-*/
.env
.env.*
!.env.example

# Local-only notes, scratchpads, audit lists, etc.
*.local.md

# Local rstudio preference overrides
.prefs.local.json
